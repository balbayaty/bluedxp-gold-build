apiVersion: v1
kind: Service
metadata:
  name: {{ include "bluedxp.fullname" . }}
  labels:
    {{- include "bluedxp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bluedxp.selectorLabels" . | nindent 4 }}

