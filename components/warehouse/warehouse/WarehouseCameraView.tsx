"use client";

/**
 * Compatibility shim: keep legacy import paths working.
 *
 * Canonical implementation:
 * - `components/warehouse/WarehouseCameraView.tsx`
 */

export * from "../WarehouseCameraView";
export { default } from "../WarehouseCameraView";
